#!/bin/bash

git log \
    --color \
    --graph \
    --pretty=format:'%Cred%h%Creset →%C(yellow)%d%Creset %s ③%Cgreen%cr ③%C(bold blue)%an %CredSIG:%G?%GS%Creset' \
    --abbrev-commit \
    $* | sed -f ${CLOVIS_CONFIG:-$HOME/config}/sed-git-log.sed | column -t -s '③' | less -rFX
